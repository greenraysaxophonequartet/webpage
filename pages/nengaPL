<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>推しお守り年賀状 | YouTubeメンバーシップ限定</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&family=Noto+Serif+JP:wght@400;600;700&display=swap" rel="stylesheet">
    
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        /* --- Reset & Variables --- */
        :root {
            --c-base: #FAFAFA;      /* 和紙のような白 */
            --c-main: #D3381C;      /* 朱色 */
            --c-gold: #C5A059;      /* 落ち着いた金 */
            --c-text: #2D2D2D;      /* 濃い墨色 */
            --c-text-light: #666;   /* 薄墨色 */
            --c-accent-bg: #FDF5F5; /* 薄い赤の背景 */
            
            --f-serif: 'Noto Serif JP', serif;
            --f-sans: 'Noto Sans JP', sans-serif;
            
            --shadow: 0 4px 20px rgba(0,0,0,0.05);
            --radius: 8px;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: var(--f-sans);
            color: var(--c-text);
            background-color: var(--c-base);
            line-height: 1.8;
            overflow-x: hidden;
            background-image: radial-gradient(#E8E8E8 1px, transparent 1px);
            background-size: 20px 20px; /* 霞のようなドットパターン */
        }

        /* --- Typography --- */
        h1, h2, h3 {
            font-family: var(--f-serif);
            font-weight: 700;
        }

        .vertical-text {
            writing-mode: vertical-rl;
            text-orientation: upright;
            letter-spacing: 0.15em;
        }

        .text-center { text-align: center; }
        .text-gold { color: var(--c-gold); }
        .text-main { color: var(--c-main); }

        /* --- Components --- */
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 0 20px;
            position: relative;
            background: var(--c-base);
            box-shadow: 0 0 50px rgba(0,0,0,0.03);
            min-height: 100vh;
        }

        .btn {
            display: inline-block;
            background-color: var(--c-main);
            color: white;
            font-family: var(--f-sans);
            font-weight: 700;
            text-decoration: none;
            padding: 16px 40px;
            border-radius: 50px;
            box-shadow: 0 4px 15px rgba(211, 56, 28, 0.3);
            transition: transform 0.2s, box-shadow 0.2s;
            letter-spacing: 0.05em;
            width: 100%;
            max-width: 320px;
            text-align: center;
            border: 2px solid var(--c-main);
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(211, 56, 28, 0.4);
            background-color: #be3219;
        }

        .btn-outline {
            background-color: transparent;
            color: var(--c-main);
        }
        .btn-outline:hover {
            background-color: var(--c-main);
            color: white;
        }

        .section {
            padding: 80px 0;
            position: relative;
        }

        .section-title {
            text-align: center;
            font-size: 24px;
            margin-bottom: 40px;
            position: relative;
            display: inline-block;
            left: 50%;
            transform: translateX(-50%);
        }
        
        /* 水引あしらい（タイトル上下） */
        .section-title::before,
        .section-title::after {
            content: '';
            display: block;
            width: 40px;
            height: 2px;
            background: var(--c-gold);
            margin: 10px auto;
        }

        /* --- Animations --- */
        .fade-in {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.8s ease-out, transform 0.8s ease-out;
        }
        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* --- Header / Hero --- */
        .hero {
            height: 90vh; /* スマホ画面いっぱいに */
            max-height: 800px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            background: linear-gradient(180deg, rgba(255,255,255,0) 0%, rgba(211,56,28,0.03) 100%);
            border-bottom: 1px solid rgba(0,0,0,0.05);
            overflow: hidden;
        }

        /* 和風の枠線 */
        .hero::before {
            content: '';
            position: absolute;
            top: 20px; left: 20px; right: 20px; bottom: 20px;
            border: 1px solid var(--c-gold);
            opacity: 0.5;
            pointer-events: none;
        }

        .hero-sub {
            font-size: 14px;
            color: var(--c-gold);
            letter-spacing: 0.1em;
            margin-bottom: 40px;
            font-weight: 700;
            text-transform: uppercase;
        }

        .hero-main {
            display: flex;
            flex-direction: row-reverse; /* 縦書きを右から左へ */
            gap: 20px;
            margin-bottom: 60px;
        }

        .hero-main h1 {
            font-size: 32px;
            line-height: 1.5;
            color: var(--c-text);
            height: 300px; /* 縦書き領域の高さ確保 */
        }
        
        .hero-main .accent {
            color: var(--c-main);
        }

        .hero-stamp {
            position: absolute;
            top: 15%;
            right: 10%;
            width: 80px;
            height: 80px;
            border: 2px solid var(--c-main);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--c-main);
            font-family: var(--f-serif);
            font-weight: 700;
            transform: rotate(15deg);
            opacity: 0.8;
            font-size: 14px;
            z-index: 0;
            background-color: rgba(255,255,255,0.8);
        }

        /* --- Concept --- */
        .concept p {
            max-width: 500px;
            margin: 0 auto 30px;
            text-align: center;
            font-size: 16px;
        }
        
        .concept-message {
            font-family: var(--f-serif);
            font-size: 18px;
            line-height: 2.2;
        }

        /* --- Features --- */
        .features-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 30px;
            max-width: 600px;
            margin: 0 auto;
        }

        .feature-card {
            background: white;
            padding: 30px;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            text-align: center;
            border-top: 4px solid var(--c-main);
        }

        .feature-icon {
            color: var(--c-main);
            margin-bottom: 20px;
        }

        .feature-card h3 {
            font-size: 18px;
            margin-bottom: 15px;
            color: var(--c-text);
        }

        .feature-card p {
            font-size: 14px;
            color: var(--c-text-light);
            text-align: left;
        }

        /* --- Spec --- */
        .spec-table {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            border-collapse: collapse;
        }

        .spec-table th, .spec-table td {
            padding: 15px;
            border-bottom: 1px solid #eee;
            text-align: left;
        }
        
        .spec-table th {
            width: 30%;
            color: var(--c-main);
            font-weight: 700;
            font-family: var(--f-serif);
        }

        /* --- Flow --- */
        .timeline {
            max-width: 500px;
            margin: 0 auto;
            position: relative;
            padding-left: 30px;
        }

        .timeline::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 2px;
            background: #eee;
        }

        .timeline-item {
            margin-bottom: 40px;
            position: relative;
        }

        .timeline-dot {
            position: absolute;
            left: -35px;
            top: 0;
            width: 12px;
            height: 12px;
            background: var(--c-main);
            border-radius: 50%;
            border: 2px solid white;
            box-shadow: 0 0 0 2px var(--c-main);
        }

        .timeline-date {
            font-size: 14px;
            color: var(--c-gold);
            font-weight: 700;
            margin-bottom: 5px;
            display: block;
        }

        .timeline-content h3 {
            font-size: 16px;
            margin-bottom: 5px;
        }

        .timeline-content p {
            font-size: 14px;
            color: var(--c-text-light);
        }

        /* --- Caution --- */
        .caution-box {
            background-color: #fff;
            border: 2px solid var(--c-main);
            padding: 30px;
            max-width: 600px;
            margin: 0 auto;
            border-radius: var(--radius);
            position: relative;
        }
        
        .caution-box::after {
            content: '重要';
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--c-main);
            color: white;
            padding: 2px 15px;
            font-size: 12px;
            font-weight: bold;
            border-radius: 20px;
        }

        .caution-list {
            list-style: none;
        }

        .caution-list li {
            margin-bottom: 10px;
            font-size: 14px;
            padding-left: 1.2em;
            text-indent: -1.2em;
        }

        .caution-list li::before {
            content: '⚠️';
            margin-right: 5px;
        }

        /* --- Footer --- */
        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 60px 20px;
            margin-top: 40px;
        }
        
        .footer-logo {
            font-family: var(--f-serif);
            font-size: 20px;
            margin-bottom: 20px;
        }

        /* --- Mizuhiki Divider (SVG) --- */
        .mizuhiki-divider {
            width: 100%;
            height: 40px;
            margin: 0 auto;
            display: block;
            color: var(--c-main);
            opacity: 0.3;
        }

        /* --- Responsive Tweaks --- */
        @media (min-width: 768px) {
            .hero-main h1 { font-size: 42px; height: 400px; }
            .features-grid { grid-template-columns: repeat(3, 1fr); }
            .section { padding: 100px 0; }
        }
    </style>
</head>
<body>

    <div class="container">
        
        <!-- Header / Hero Section -->
        <header class="hero">
            <div class="hero-stamp">限定<br>企画</div>
            <p class="hero-sub fade-in">YouTube Membership Project 2025</p>
            
            <div class="hero-main fade-in">
                <!-- 縦書きコピー -->
                <h1 class="vertical-text">
                    推しから届く、<br>
                    <span class="accent">新年のお守り。</span>
                </h1>
            </div>

            <div class="fade-in" style="margin-top: 40px; width: 100%; text-align: center;">
                <a href="#buy" class="btn">お守り年賀状を受け取る</a>
                <p style="font-size: 12px; margin-top: 10px; color: var(--c-text-light);">※YouTubeメンバーシップ限定</p>
            </div>
        </header>

        <!-- Concept Section -->
        <section class="section concept">
            <h2 class="section-title fade-in">ご挨拶</h2>
            
            <div class="fade-in concept-message">
                <p>いつも応援ありがとうございます。<br>
                2024年は、皆さんのおかげで<br>素敵な1年になりました。</p>

                <p>新しい2025年も、<br>皆さんが笑顔で過ごせますように。</p>

                <p>そんな願いを込めて、<br>お守り代わりの年賀状をお届けします。</p>

                <p>メンバーの皆様へ、感謝の気持ちを込めて。<br>
                一枚一枚、直筆で宛名とサインを<br>入れさせていただきます。</p>
            </div>
        </section>

        <!-- Divider -->
        <svg class="mizuhiki-divider" viewBox="0 0 100 20" preserveAspectRatio="none">
            <path d="M0,10 Q25,20 50,10 T100,10" fill="none" stroke="currentColor" stroke-width="0.5"/>
            <path d="M0,10 Q25,0 50,10 T100,10" fill="none" stroke="#C5A059" stroke-width="0.5"/>
        </svg>

        <!-- Features Section -->
        <section class="section features">
            <h2 class="section-title fade-in">こだわりの仕様</h2>
            <div class="features-grid">
                <!-- Feature 1 -->
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i data-lucide="pen-tool" width="40" height="40"></i></div>
                    <h3>あなたのお名前を<br>直筆で</h3>
                    <p>あなたの本名、またはハンドルネームを、推しが心を込めて手書きします。世界に一枚だけの年賀状です。</p>
                </div>
                <!-- Feature 2 -->
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i data-lucide="heart-handshake" width="40" height="40"></i></div>
                    <h3>メンバー<br>直筆サイン入り</h3>
                    <p>印刷ではありません。一枚一枚、直書きしたサインをお届けします。感謝の気持ちを筆に乗せて。</p>
                </div>
                <!-- Feature 3 -->
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i data-lucide="stamp" width="40" height="40"></i></div>
                    <h3>特製「お守り」<br>デザイン</h3>
                    <p>2025年を健やかに過ごせるよう、御札をモチーフにした和モダンなデザイン。デスクに飾れるお守りです。</p>
                </div>
            </div>
        </section>

        <!-- Spec Section -->
        <section class="section bg-accent">
            <h2 class="section-title fade-in">商品情報</h2>
            <table class="spec-table fade-in">
                <tr>
                    <th>商品名</th>
                    <td>YouTubeメンバー限定<br>推しお守り年賀状</td>
                </tr>
                <tr>
                    <th>価格</th>
                    <td style="font-size: 18px; font-weight: bold;">1,000円 <span style="font-size: 12px; font-weight: normal;">(税込・送料込)</span></td>
                </tr>
                <tr>
                    <th>サイズ</th>
                    <td>一般ハガキサイズ</td>
                </tr>
                <tr>
                    <th>販売対象</th>
                    <td>YouTubeメンバーシップ加入者様のみ</td>
                </tr>
            </table>
        </section>

        <!-- Flow Section -->
        <section class="section flow">
            <h2 class="section-title fade-in">お届けまでの流れ</h2>
            <div class="timeline fade-in">
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <span class="timeline-date">12月中旬〜</span>
                    <div class="timeline-content">
                        <h3>ご注文受付（BASE）</h3>
                        <p>専用URLよりご購入ください。</p>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <span class="timeline-date">受付終了後</span>
                    <div class="timeline-content">
                        <h3>制作期間</h3>
                        <p>心を込めて、宛名とサインを記入します。</p>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <span class="timeline-date">12月25日頃</span>
                    <div class="timeline-content">
                        <h3>投函予定</h3>
                        <p>元旦到着を目指して投函します。</p>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <span class="timeline-date">1月1日〜3日</span>
                    <div class="timeline-content">
                        <h3>到着</h3>
                        <p>ポストをご確認ください。</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Caution Section -->
        <section class="section caution">
            <div class="caution-box fade-in">
                <h3 class="text-center" style="margin-bottom: 20px;">必ずお読みください</h3>
                <ul class="caution-list">
                    <li><strong>メンバーシップ限定商品です。</strong> 購入後にメンバー確認が取れない場合、キャンセルとなる可能性があります。</li>
                    <li><strong>到着時期について：</strong> 郵便事情により、元旦に届かない場合があります。予めご了承ください。</li>
                    <li><strong>住所不備にご注意ください：</strong> 住所間違いによる不着・再送は対応できない場合があります。必ず正確にご入力ください。</li>
                </ul>
            </div>
        </section>

        <!-- Footer -->
        <footer id="buy">
            <p class="footer-logo">推しお守り年賀状 2025</p>
            <p style="margin-bottom: 30px;">新しい一年も、あなたのそばに。</p>
            
            <a href="https://greenraysq.thebase.in/" class="btn" style="background-color: white; color: var(--c-main);">BASE販売ページへ移動する</a>
            
            <p style="font-size: 10px; margin-top: 40px; color: #888;">&copy; 2024 Your Channel Name. All Rights Reserved.</p>
        </footer>

    </div>

    <script>
        // Icons Initialization
        lucide.createIcons();

        // Fade-in Animation Observer
        const observerOptions = {
            root: null,
            rootMargin: '0px',
            threshold: 0.1
        };

        const observer = new IntersectionObserver((entries, observer) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    observer.unobserve(entry.target); // Run once
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });
    </script>
</body>
</html>
